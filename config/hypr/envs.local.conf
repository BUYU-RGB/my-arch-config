# Local machine overrides.
# Keep this file empty by default so the repo stays hardware-neutral.
#
# NVIDIA example:
# env = LIBVA_DRIVER_NAME,nvidia
# env = __GLX_VENDOR_LIBRARY_NAME,nvidia
#
# Locale example:
# env = LANG,zh_CN.UTF-8
# env = LC_CTYPE,zh_CN.UTF-8
